1. Get the application URL by running these commands:
{{- if .Values.ingress.enabled }}
{{- range $host := .Values.ingress.hosts }}
  {{- range .paths }}
  http{{ if $.Values.ingress.tls }}s{{ end }}://{{ $host.host }}{{ .path }}
  {{- end }}
{{- end }}
{{- end }}

2. Database Configuration:
{{- if .Values.postgresql.enabled }}
   📊 Database: PostgreSQL Standalone
   🔌 Service: {{ include "prima-nota.fullname" . }}-postgresql:5432
{{- else if .Values.cloudnativepg.enabled }}
   📊 Database: CloudNativePG Cluster ({{ .Values.cloudnativepg.cluster.instances }} instances)
   🔌 Read-Write: {{ .Values.cloudnativepg.cluster.name | default (printf "%s-postgres" (include "prima-nota.fullname" .)) }}-rw:5432
   🔌 Read-Only:  {{ .Values.cloudnativepg.cluster.name | default (printf "%s-postgres" (include "prima-nota.fullname" .)) }}-ro:5432
   {{- if .Values.cloudnativepg.cluster.backup.enabled }}
   💾 Backup: Enabled ({{ .Values.cloudnativepg.cluster.backup.retentionPolicy }})
   {{- end }}
{{- else if .Values.externalDatabase.enabled }}
   📊 Database: External Database
   🔌 Service: {{ .Values.externalDatabase.host }}:{{ .Values.externalDatabase.port }}
{{- end }}

3. Access the application:
   - Frontend: Access via ingress URL above
   - PgAdmin: {{ if .Values.ingress.enabled }}https://{{ (index .Values.ingress.hosts 0).host }}/pgadmin{{ else }}Port-forward required{{ end }}
